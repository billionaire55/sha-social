name: SHA Blog Promo Post
on:
  repository_dispatch:
    types: [blog-published]
  workflow_dispatch:
    inputs:
      title:
        description: "Blog post title (leave blank to skip social posting)"
        required: false
        type: string
      excerpt:
        description: "Blog post excerpt"
        required: false
        type: string
      url:
        description: "Blog post URL"
        required: false
        type: string
permissions:
  contents: write
jobs:
  promo:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: |
          rm -f package-lock.json
          rm -rf node_modules
          npm install
          npm install --os=linux --cpu=x64 --libc=glibc sharp --no-save
      - name: Generate blog promo image
        env:
          BLOG_TITLE: ${{ github.event.client_payload.title || github.event.inputs.title || 'Test Post (image only, not posted)' }}
          BLOG_EXCERPT: ${{ github.event.client_payload.excerpt || github.event.inputs.excerpt || 'Test excerpt' }}
        run: node --input-type=module < generate-blog-promo-card.mjs
      - name: Commit promo image
        run: |
          git config user.name "sha-bot"
          git config user.email "bot@smarterhustleacademy.com"
          git add blog_promo_image.png || true
          git commit -m "Blog promo image $(date +%Y-%m-%d)" || echo "nothing to commit"
          git push || echo "no push"
      - name: Post to social platforms
        if: github.event_name == 'repository_dispatch' || github.event.inputs.title != ''
        env:
          POSTPROXY_API_KEY: ${{ secrets.POSTPROXY_API_KEY }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          BLOG_TITLE: ${{ github.event.client_payload.title || github.event.inputs.title }}
          BLOG_URL: ${{ github.event.client_payload.url || github.event.inputs.url }}
        run: node --input-type=module < post-blog-promo.mjs
